<h1>Hello To MoCourses Website</h1><br />
<% @course.each{|k| puts k.course_title}%>

<table border="1">
  <thead>
    <tr>
      <th>Title</th>
    </tr>
  </thead>

  <tbody>
    <% @course.each do |course| %>
      <tr>
        <td><%= raw(course.course_title) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Course', new_course_path %>
